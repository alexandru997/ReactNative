import React from 'react';
import {SafeAreaView, StatusBar, Text} from "react-native";

const Home = () => {

    return (
        <SafeAreaView>
            <StatusBar hidden={true}/>
            <Text>Hello</Text>
        </SafeAreaView>
    );
};

export default Home;
